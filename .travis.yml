language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t iojoe/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI= true iojoe/docker-react npm run test -- --coverage
